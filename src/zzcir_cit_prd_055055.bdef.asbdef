managed implementation in class ZZCIBP_R_CIT_PRD_055055 unique;
strict ( 2 );
with draft;
extensible;
define behavior for ZZCIR_CIT_PRD_055055 alias ZzcirCitPrd055055
persistent table ZCIT_PRD_055
extensible
draft table ZZCICT_PRD_D055
etag master LocalLastChangedAt
lock master total etag LastChangedAt
authorization master( global )
{
  field ( mandatory : create )
   ProductID;

  field ( readonly )
   LocalCreatedBy,
   LocalCreatedAt,
   LocalLastChangedBy,
   LocalLastChangedAt,
   LastChangedAt;

  field ( readonly : update )
   ProductID;


  create;
  update;
  delete;

  draft action Activate optimized;
  draft action Discard;
  draft action Edit;
  draft action Resume;
  draft determine action Prepare;

  mapping for ZCIT_PRD_055 corresponding extensible
  {
    ProductID = PRODUCT_ID;
    ProductName = PRODUCT_NAME;
    Category = CATEGORY;
    Quantity = QUANTITY;
    Price = PRICE;
    Currency = CURRENCY;
    LocalCreatedBy = LOCAL_CREATED_BY;
    LocalCreatedAt = LOCAL_CREATED_AT;
    LocalLastChangedBy = LOCAL_LAST_CHANGED_BY;
    LocalLastChangedAt = LOCAL_LAST_CHANGED_AT;
    LastChangedAt = LAST_CHANGED_AT;
  }

}